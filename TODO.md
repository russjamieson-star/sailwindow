# SailWindow — TODO
_Last updated: 2026-07-20_

## Great Lakes & Mississippi — Stripe pricing (done), app content (not started)
- [x] **4 new Stripe products + 6 Payment Links created (2026-07-20).** Pricing infrastructure only — no app files exist yet for either edition. Created live in Stripe Dashboard (account: Rj & HRS Marketing LLC, `acct_1TfTvK80XXRgQXiK`):
  - "SailWindow Great Lakes": $5.99/mo (https://buy.stripe.com/4gMcMZaOMfyMfaKb2M7Re05), $49/yr (https://buy.stripe.com/9B69AN0a81HW9Qqb2M7Re06)
  - "SailWindow Mississippi": $5.99/mo (https://buy.stripe.com/00w14haOM3Q46Ee5Is7Re07), $49/yr (https://buy.stripe.com/3cI9AN2igcmAfaK4Eo7Re08)
  - "SailWindow 3-Edition Bundle": $15/mo (https://buy.stripe.com/3cI5kxcWU3Q46Eec6Q7Re09)
  - "SailWindow 4-Edition Bundle (Grand Loop All-Access)": $20/mo (https://buy.stripe.com/7sY5kx5usaes4w63Ak7Re0a)
  - All 12 payment links (these 6 + the prior 6 for Pro/Atlantic/Bundle) verified present on the Payment Links list page.
- [ ] **Not started, deferred:** actual Great Lakes and Mississippi app scaffolding (fork `dist/index.html` → `dist-greatlakes/`, `dist-mississippi/`), real stations/marinas/locks data (Great Lakes: no NOAA tide predictions, IGLD datum, seasonal ice closure, lock-based chokepoints; Mississippi: USGS/Corps river-stage gauges instead of NOAA tides, lock-and-dam data, barge traffic). See CLAUDE.md's Long-Range Roadmap section for the full architecture notes on why this needs real research before any code is written (the NOAA CO-OPS tide API that Atlantic/Gulf use won't work as-is for either new edition). With the shared-engine refactor now in place (see below), each new edition only needs its own `config.<edition>.js` + a new conditions-provider module — not a fork of the whole app.
- [ ] **Not decided:** whether/how to wire the new Great Lakes/Mississippi/3-Edition/4-Edition Stripe links into any live pricing UI. The 2 existing editions' subscribe modal only knows `'monthly'/'annual'/'bundle'` — adding Trio/Quad options would make purchasable tiers for editions with no content live, which needs a decision from Russ first.

## Refactor — shared engine + per-edition config (done, cut over 2026-07-20)
- [x] **`shared/engine.js` + `shared/config.gulf.js` + `shared/config.atlantic.js` built and verified (2026-07-20).** Diffed every function in Gulf's `dist/index.html` against Atlantic's `dist-atlantic/index.html`: 118 of 126 functions (scoring, tide/weather fetch, marina cards, trial/entitlement system, most of the Stripe checkout flow) were already byte-identical between editions — a straight copy-paste fork. 8 more (`activateSubscription`, `buildMarinaCards`, `chooseFreeMode`, `handleStripeCheckout`, `marinaPhoto`, `nearestStationId`, `openSubscribeModal`, `resetPrefs`) differed only in edition-specific literals — parameterized via a global `EDITION_CONFIG` object. Verified behaviorally (Node `vm` sandbox + real headless-browser load) that every parameterized function's output matches the original live code exactly for both Gulf and Atlantic.
  - Deliberately NOT touched: `LOCATIONS`/`MARINA_META`/`MARINA_PHOTOS` (real marina content), `DRAWBRIDGES`/`BRIDGE_DATA`/bridge-view functions (bridges vs. locks vs. river gauges differ too much per body of water), `submitWaitlist` (Gulf-only — it's Gulf's own page promoting the Atlantic edition to Gulf visitors).
- [x] **Cut over into live files (2026-07-20).** `dist/index.html` 3785 → 2103 lines, `dist-v8/index.html` identical (was byte-identical to `dist` before), `dist-atlantic/index.html` 3538 → 1859 lines. `engine.js` copied into each dist folder (Cloudflare Pages deploys each independently — no shared static hosting between them) plus a canonical copy under `shared/`. Verified in a real headless-Chromium load of both slimmed-down pages (not just Node): `EDITION_CONFIG.edition` resolves correctly per edition, `nearestStationId`/`activateSubscription`/`openSubscribeModal` all produce the exact right output, zero console errors. Nothing committed/pushed yet — still sitting as uncommitted changes in the repo, Russ's call when to commit.
- [x] **Relative-path bug found and fixed (2026-07-20).** All three dist files loaded `engine.js` and `marina_data.json` via paths relative to the current page (`<script src="engine.js">`, `fetch('marina_data.json')`). Harmless while every page lived at the site root, but it broke the moment a page sat one folder deeper — confirmed via a real Playwright load of a new `/locations/{slug}.html` page: 404 on `/locations/engine.js`, page errors (`storageAvailable is not defined`, `loadPrefsFromStorage is not defined`). Fixed by switching to root-absolute paths (`/engine.js`, `/marina_data.json`) in `dist/index.html`, `dist-v8/index.html`, and `dist-atlantic/index.html`. Re-verified via Playwright: a nested location page now loads with zero 404s and zero page errors, and the root `index.html` still works correctly (45 marinas, correct restaurant data) after the change.
- [ ] **Future editions:** Great Lakes/Mississippi will need a new conditions-provider module (lake level / river stage) alongside `engine.js`, not a retrofit of the NOAA tide-fetch functions already in it — noted in `engine.js`'s header comment.

## Atlantic Edition — before it can launch
- [x] **Bridge Schedules reference panel fixed (2026-07-20).** `BRIDGE_DATA` (the "ICW Bridge Schedules" help/reference panel) was still the old 48-entry Gulf/TX/LA/MS/AL/FL dataset — never replaced during the fork. Fixed by making `BRIDGE_DATA` a computed `.map()` over `DRAWBRIDGES` instead of a hand-duplicated second array, so the two can never drift apart again. Filter chips/categories replaced with the Atlantic edition's existing 6-leg geography (Georgia to North Carolina / Mid-Atlantic & Chesapeake / C&D Canal & New Jersey / NY Harbor & Long Island Sound / Rhode Island to Massachusetts / Maine Coastline) in place of the Gulf-specific `gulf_icw`/`atlantic_icw`/`panhandle`/`tx_la` chips. Verified via Node: all 32 DRAWBRIDGES entries map 1:1 to BRIDGE_DATA with no drift and no id collisions (11 GA–NC, 2 Mid-Atlantic, 5 C&D/NJ, 6 NY/LI, 4 RI–MA, 4 Maine).
- [x] **Duplicate DOM `id="bridge-list"` bug fixed (2026-07-20).** The static full "Bridge Schedules" view container and the dynamically-inserted "nearby bridges" widget list both used `id="bridge-list"` — whichever was first in DOM order silently won for both features' `getElementById` lookups. Renamed the nearby-widget's instance to `bridge-list-nearby` in `renderBridges()` and `toggleBridges()`; the full Bridge Schedules view keeps `#bridge-list` untouched.
- [x] **Bridge coverage north of NJ — done (2026-07-20).** 17 more bridges sourced from Russ's `Northeast_Movable_Bridges_Sailwindow.xlsx`, covering NJ through ME. `DRAWBRIDGES` is now 32 total, GA → ME.
- [x] **Atlantic Stripe Payment Links created (2026-07-20).** Real "SailWindow Atlantic" product ($5.99/mo + $49/yr), matching Gulf's pricing. `STRIPE_MONTHLY`/`STRIPE_ANNUAL` in `dist-atlantic/index.html` no longer point at Gulf's links.
- [x] **$10/mo Bundle Stripe Payment Link created (2026-07-20).** `STRIPE_BUNDLE` is the same real link in `dist/index.html` (+ `dist-v8/index.html`) and `dist-atlantic/index.html` — no longer a `"#"` placeholder.
- [x] **`FREE_KEYS` confirmed by Russ (2026-07-20).** Keeping the placeholder as-is: Annapolis + Newport, a two-region free sampler (Chesapeake + New England), mirroring Gulf's Tampa + Pensacola free-tier pattern. No code change needed — already the live value in `EDITION_CONFIG.freeKeys`.
- [x] **Atlantic `marina_data.json` restaurant data populated (2026-07-20).** All 45 marinas had `restaurants: []`; sourced real data via a new `collect-atlantic-restaurants.py` (same Google Places API pipeline as Gulf's `collect-marina-restaurants.py`, run by Russ with his own `GOOGLE_PLACES_KEY` — cost ~$1.44). Result: 495 restaurants across 45 marinas (7 marinas came back empty — rural marinas with nothing in the 800m search radius, same pattern as ~21 of Gulf's 92). 88% have a website, 91% a phone number, 96% a rating. Verified in a real headless-browser load: `getSwRestaurants()` returns correctly-shaped, real restaurant data (spot-checked Annapolis City Dock against actual known restaurants). The key used has since been rotated/deleted by Russ after being pasted in chat during troubleshooting.
  - Note: this is bulk Google-sourced listing data only, all `partner: false`. The actual partner/discount outreach program (see `restaurants/SailWindow Partner Program Guide.pdf` + outreach email templates) is a separate manual process Russ runs himself — Gulf's real data shows it's never been activated for any marina either (0 of 806 Gulf restaurants are marked partner).
- [x] **21 Atlantic SEO location pages generated (2026-07-20).** One per `LOCATIONS` entry (Atlantic's catalog is 21 entries, matching its 21 NOAA stations 1:1 — unlike Gulf's broader 60-city approach): `fort-pulaski`, `charleston-cooper-river-entrance`, `wilmington`, `sewells-point-norfolk`, `annapolis`, `baltimore-fort-mchenry`, `reedy-point`, `cape-may`, `atlantic-city`, `the-battery-ny-harbor`, `kings-point`, `bridgeport`, `new-london`, `newport`, `providence`, `woods-hole`, `boston`, `nantucket-island`, `portland`, `bar-harbor`, `eastport`. Each page is the fixed (absolute-path) `dist-atlantic/index.html` with unique `<title>`/meta description/OG tags and `window.PRESELECT_LOCATION` set. Verified in a real headless-browser load with zero console errors.
- [x] **`sitemap.xml` / `robots.txt` created for Atlantic (2026-07-20).** Atlantic had neither before. New `dist-atlantic/sitemap.xml` lists the homepage + all 21 location URLs (`https://atlantic.sailwindow.com/locations/{slug}`); new `dist-atlantic/robots.txt` allows all + points at the sitemap.
- [x] **`dist-atlantic/` deployed to Cloudflare Pages, `atlantic.sailwindow.com` set up (2026-07-20).** Russ ran `npx wrangler pages deploy dist-atlantic --project-name=sailwindow-atlantic --branch main` from his own Terminal (first attempt accidentally deployed to a `mainseems` preview branch due to a garbled paste — harmless, just a stray preview deployment sitting alongside production; re-ran with the correct `--branch main` and it succeeded). Production is live at `https://sailwindow-atlantic.pages.dev` — verified in a real browser: `EDITION_CONFIG.edition === "atlantic"`, 45 marinas loaded, a nested `/locations/annapolis` page loads with zero console errors. Custom domain `atlantic.sailwindow.com` added via the Cloudflare dashboard wizard (CNAME → `sailwindow-atlantic.pages.dev`), status "Initializing" as of this writing — since `sailwindow.com` is already on Cloudflare's own DNS this should activate within minutes to an hour, not the full 48-hour window the UI warns about generically.
- [ ] 10 of 45 Atlantic marinas are geocoded to town-center precision rather than exact street address (Russ confirmed this is fine — captains will identify the marina by sight on approach).

## Gulf + Atlantic bundle (the $10/mo combined plan)
- [ ] **No shared entitlement backend.** Gulf and Atlantic are separate subdomains with separate browser storage, so a bundle subscriber has to tap "Activate" once on sailwindow.com *and* once on atlantic.sailwindow.com. The checkout flow now explains this, but it's a manual two-step, not seamless. A real fix needs a small shared verification backend (e.g. checking one Stripe subscription from both sites) if this friction becomes a problem.
- [x] **Waitlist backend fully wired and verified live (2026-07-20).** Built as a shared, edition-parameterized system (one Sheet + one Apps Script serve Atlantic, and later Great Lakes/Mississippi, tagged by an `Edition` column) rather than a one-off — see CLAUDE.md. `waitlist-Code.gs` (previously only described, never actually written to the repo) is now saved at the repo root and deployed by Russ as an Apps Script Web App. `WAITLIST_SCRIPT_URL` in `dist/index.html` and `dist-v8/index.html` now points at the real deployed URL (`https://script.google.com/macros/s/AKfycby.../exec`). **Verified end-to-end**: sent a real test POST from a live browser tab (same `fetch()` call the site's `submitWaitlist()` uses) and confirmed the row landed in the "SailWindow — Waitlist" Sheet (`Timestamp | Edition: TEST-VERIFY | Email: claude-test-verification@example.com | Source: verification-script`) — Russ can delete that one test row whenever convenient. Real submissions now actually reach the sheet instead of showing "Waitlist isn't wired up yet."

## Gulf Edition — open items
- [x] **`marina_data.json` restored to live production (2026-07-20).** It existed in the older `dist-v6/` build (806 restaurants across 92 marinas) but was missing from the current live `dist/` and `dist-v8/` — the "Ashore" restaurant panel had nothing to show. Copied the same 92-marina/806-restaurant file into `dist/marina_data.json` and `dist-v8/marina_data.json` (schema-checked: 0 issues, matches the same `{name, address, phone, website, rating, google_maps, partner, discount_offer}` shape used for Atlantic). Bumped `EDITION_CONFIG.dbVersion` "2026-06-29" → "2026-07-20" in both dist files so any stale localStorage cache re-fetches. Verified in a real headless-browser load: `getSwRestaurants()` returns 92 marinas loaded, Kemah Boardwalk Marina correctly returns 20 restaurants (first: Eculent), zero console errors, zero 404s.
- [ ] Sanity-check the new hero badge wording ("NOAA + ECMWF · FL Gulf & Atlantic Coasts") reads clearly to real visitors — it's a subtle rewording meant to stop confusion with the new Atlantic Edition banner sitting right above it.

## Stripe / billing hygiene
- [x] **Cleaned up duplicate/test subscriptions (2026-07-20).** Of 16 active subscriptions, only 1 (cbc4windows@me.com, annual) was a real paying customer — the other 15 were Russ's own testing (russjam2000@yahoo.com ×8, russjamieson@gmail.com ×1) or his tester's (jkaiserski@mac.com ×6). All 15 canceled immediately in Stripe. Current active subscriber count: 1.

## Backups
- [x] **Google Drive "SailWindow BU" location resolved (2026-07-20).** It's under russjamieson@gmail.com (the 5TB personal Drive), not sailwindow.com, and not the account synced to this Mac's local `Google Drive` folder — that's why it wasn't found there on 2026-07-19. Backing it up now goes through Claude's Google Drive connector directly (folder ID `1_JT5-Z5WhT5uI_KBFio4NHkJo4Gv-cML`), bypassing the local Mac mount entirely.
- [ ] GitHub push was blocked by TWO separate stale lock files this session (`index.lock`, then `HEAD.lock`) left behind by an interrupted git process. Confirm the push actually landed after clearing both — check with `git log -1` and `git status`.
- [x] **Found and cleared a third stale `.git/index.lock` (2026-07-20).** Left over from an earlier interrupted git operation; moved to `_to_delete/` in the repo (safe to delete that folder). Would have blocked any `git add`/`commit`/`push` Russ tried to run until cleared.
- [ ] **All of this session's work (refactor, Atlantic content, waitlist backend, marina_data.json restore) is still uncommitted locally.** Russ still needs to run `git add` / `git commit` / `git push` from his own Terminal — see the commit instructions given in chat.
